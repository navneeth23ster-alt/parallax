name: Aggregate headlines
on:
  schedule:
    - cron: "0 */6 * * *"   # every 6 hours
  workflow_dispatch:
permissions:
  contents: write
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install -r requirements.txt
      - run: python -m parallax run
      - name: Commit reports
        run: |
          git config user.name "parallax-bot"
          git config user.email "actions@users.noreply.github.com"
          git add reports data/runs.jsonl data/timeline.jsonl
          git diff --cached --quiet || git commit -m "reports: $(date -u +%F-%H%M)"
          git push
